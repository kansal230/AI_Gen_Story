[project]
name = "backend"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
authors = [
    { name = "kansal230", email = "87700165+kansal230@users.noreply.github.com" }
]
requires-python = ">=3.12"
dependencies = [
    "fastapi[all]>=0.141.1",
    "langchain-openai>=1.6.0",
    "psycopg2-binary>=2.9.12",
    "python-dotenv>=1.2.3",
    "sqlalchemy>=2.0.52",
    "uvicorn>=0.52.4",
]

[project.scripts]
backend = "backend:main"

[build-system]
requires = ["uv_build>=0.12.7,<0.13.0"]
build-backend = "uv_build"
